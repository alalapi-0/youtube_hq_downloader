google-api-python-client
python-dotenv
PyYAML
pandas
tqdm
isodate

# Optional: explicit HTTP for LLM client (fallback uses stdlib urllib)
requests

# Optional: probe-format / available_format_heights via yt-dlp metadata
yt-dlp
